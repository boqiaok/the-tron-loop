{
  "name": "the-tron-loop",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "migration:generate": "pnpm --filter server migration:generate",
    "migration:run": "pnpm --filter server migration:run",
    "migration:revert": "pnpm --filter server migration:revert",
    "migration:show": "pnpm --filter server migration:show"
  }
}
